# Buổi 2 (15/9/2026)

1. Hàm (review)

- Hàm lambda thường dùng với filter (lọc phần tử), map

2. List, Tuple, Dict, Set.

- List: [], ép kiểu list(a), chỉ số đánh từ 0 .. n-1, -1 --> -n
- Tuple: (), giống list, không thay đổi giá trị
- Set: các phần tử ko trùng

- Dictionary: {}, dạng key-value
  Lấy giá trị ứng với key nên dùng: mydict.get(key, default_value)

3. OOP
4. Regular Expression
